# Recipe Manager — Status

## Активная фаза

Phase 3 — миграция на Supabase (не начата); параллельно — проработка отчёта аудита кода

## Следующий шаг

Продолжить проработку `docs/audits/2026-07-19-project-audit-report.md` начиная с DEAD-2 (закрыто 33/60 — все критические + весь раздел 🟠 DOC кроме DOC-14 + UNDOC-1 + весь раздел LOGIC (1-9) + DEAD-1; осталось 27: DEAD×8, TS×7, CONV×3, PERF×8, плюс отложенная DOC-14). Phase 3 не начата, ждёт после аудита или по решению пользователя.

## Blocker

DOC-14 (установить ESLint+Prettier) заблокирована нехваткой места на диске — `npm install` падает с ENOSPC (366 МБ свободно, npm-кэш занимает 2.4 ГБ). Пользователь решил разобраться с местом сам; вернуться к DOC-14 после очистки диска.

## Обновлено

2026-07-19

---

## Итоги последней сессии

- Закрыт весь раздел LOGIC (LOGIC-1…LOGIC-9) + UNDOC-1 + DEAD-1, по одной находке за коммит, каждая отмечена `✅ Исправлено` в отчёте
  - LOGIC-1 — `onError`-колбэк добавлен в `subscribeAll`/`subscribe` всех 5 репозиториев (Firestore `onSnapshot` третий аргумент), чтобы не глотать ошибки подписки молча
  - LOGIC-2 — `timestampToISO()` логирует `console.warn` при отсутствии `createdAt` вместо тихой подстановки текущей даты
  - LOGIC-3 — `AISuggestModal.tsx` удалён полностью (154 строки): категорически недостижимый мёртвый код (React batching схлопывал `isOpen`/`isSuggesting` в один тик), рабочий inline-дубль в `TrackerView.tsx` не тронут — выбор пользователя
  - LOGIC-4 — месячный вид планера считает калории через `sumMacros()` вместо ручного `reduce`, который игнорировал `type: 'product'` записи
  - LOGIC-5 — степпер порций в карточке рецепта превращён в эфемерный калькулятор КБЖУ (`portionCount`, не трогает `selectedRecipe.servings`, ничего не сохраняет) — выбор пользователя вместо постоянного редактирования порций
  - LOGIC-6 — `fillRemaining.ts` валидирует `proteins/fats/carbs` как числа наравне с `calories`, чтобы в промпт не улетало `"undefined"`
  - LOGIC-7 — мягкий (не строгий) rate limit на Cloudflare KV признан осознанным архитектурным ограничением и задокументирован, а не переделан на Durable Objects — выбор пользователя
  - LOGIC-8 — `calculateKbzhu.ts`/`generateImage.ts` обёрнуты в try/catch → 502, унифицировано с остальными 4 worker-маршрутами (раньше падали в общий 500)
  - LOGIC-9 — отсутствие defense-in-depth по аллергенам на уровне репозиториев признано осознанным (репозитории — чистый CRUD, готовый к замене на Supabase) и задокументировано в CLAUDE.md — выбор пользователя
  - DEAD-1 — `LocalStorageNutritionPlanRepository.ts` (нигде не подключён) и его тест удалены полностью — выбор пользователя
- Каждая находка — отдельный коммит с указанием ID; докам (отчёт аудита + CLAUDE.md + Application_description.md) — отдельные коммиты после каждого фикса

## Ключевые решения, влияющие на следующий шаг

- LOGIC-5, LOGIC-7, LOGIC-9 и DEAD-1 были явными развилками — во всех явно спрашивали пользователя, не решали единолично; паттерн продолжает работать для оставшихся 27 находок (DEAD×8, TS×7, CONV×3, PERF×8)
- Rate limit на KV и отсутствие allergy-проверки в репозиториях — оба зафиксированы как осознанные, а не как баги: пункты добавлены в `CLAUDE.md` → Known constraints, чтобы не всплывали как новые находки в будущих аудитах
- `worker/` не имеет тестовой инфраструктуры (только `tsc --noEmit`) — для находок в `worker/src/**` верификация ограничивается typecheck, регрессионные тесты не добавляются
- Repository pattern уже реализован — для Supabase нужны только новые реализации интерфейсов из `src/services/`
- Feature flag `VITE_BACKEND=firebase|supabase` переключает бэкенд в `src/infrastructure/createRepositories.ts`

---

## Где искать контекст

- `ROADMAP.md` — активные + будущие фазы (Phase 3, 4, 5)
- `Application_description.md` — бизнес-логика (6 вкладок, AI-правила, UX)
- `Technical_Project_Documentation.md` — архитектура, стек, файловая структура, env vars
- `docs/roadmap-archive/` — завершённые фазы (0a, 0b, 1, 2) + журнал решений
- `docs/superpowers/specs/` — спеки фаз
