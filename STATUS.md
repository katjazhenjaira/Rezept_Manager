# Recipe Manager — Status

## Активная фаза
Phase 3 — миграция на Supabase (не начата); параллельно — проработка отчёта аудита кода

## Следующий шаг
Продолжить проработку `docs/audits/2026-07-19-project-audit-report.md` начиная с UNDOC-1 (закрыто 22/60 — все критические + весь раздел 🟠 DOC кроме DOC-14; осталось 38: UNDOC×1, LOGIC×9, DEAD×9, TS×7, CONV×3, PERF×8, плюс отложенная DOC-14). Phase 3 не начата, ждёт после аудита или по решению пользователя.

## Blocker
DOC-14 (установить ESLint+Prettier) заблокирована нехваткой места на диске — `npm install` падает с ENOSPC (366 МБ свободно, npm-кэш занимает 2.4 ГБ). Пользователь решил разобраться с местом сам; вернуться к DOC-14 после очистки диска.

## Обновлено
2026-07-19

---

## Итоги последней сессии
- Закрыт весь раздел 🟠 «Противоречия с документацией» кроме DOC-14 (14/15): по одной находке за коммит, каждая отмечена `✅ Исправлено` в отчёте
  - DOC-1…DOC-7, DOC-11…DOC-13, DOC-15 — точечные правки `Technical_Project_Documentation.md`/`.env.example`/`docs/roadmap-archive/phase-1.md` (устаревшие сигнатуры репозиториев, названия моделей Gemini, rate-limit механизм, реликты AI Studio)
  - DOC-8 — реализована подсветка аллергенов в корзине (новый `productAllergens()` в `shared/domain/allergies.ts`, `CartView`/`CartItemRow` принимают `allergies` и подсвечивают товар + предупреждение), а не убрана из документации — выбор пользователя
  - DOC-9 — добавлена кнопка «Пересчитать КБЖУ» на карточке рецепта (`RecipesView.tsx`), вызывает `aiClient.calculateKbzhu()`, делит результат на `servings`, персистит через `recipesRepo.update()` — выбор пользователя (реализовать, не убрать из доков)
  - DOC-10 — AppHeader переведён на `useTranslation()`/`changeLanguage()` из `I18nProvider`, синхронизирован с рабочим переключателем в SettingsModal — выбор пользователя (сделать рабочим, не удалять дубль)
  - DOC-14 отложена: `npm install -D eslint ...` упал с ENOSPC (диск заполнен на 98%, свободно 366 МБ)
- Каждая находка — отдельный коммит с указанием ID (60d8302 — последний в этой сессии)

## Ключевые решения, влияющие на следующий шаг
- Три из закрытых DOC-находок (8, 9, 10) описывали документированное, но нереализованное поведение — по каждой явно спрашивали пользователя «реализовать фичу vs скорректировать документацию»; во всех трёх пользователь выбрал реализацию. Для оставшихся находок с похожей дилеммой — спрашивать так же, не решать самостоятельно
- DOC-14 (ESLint+Prettier) блокирована местом на диске, а не решением — не пропускать, вернуться после того как пользователь освободит место
- Проработка аудита идёт по одной находке за раз с отдельным коммитом на каждую — правило продолжает работать, использовать дальше для UNDOC/LOGIC/DEAD/TS/CONV/PERF
- Repository pattern уже реализован — для Supabase нужны только новые реализации интерфейсов из `src/services/`
- Feature flag `VITE_BACKEND=firebase|supabase` переключает бэкенд в `src/infrastructure/createRepositories.ts`

---

## Где искать контекст
- `ROADMAP.md` — активные + будущие фазы (Phase 3, 4, 5)
- `Application_description.md` — бизнес-логика (6 вкладок, AI-правила, UX)
- `Technical_Project_Documentation.md` — архитектура, стек, файловая структура, env vars
- `docs/roadmap-archive/` — завершённые фазы (0a, 0b, 1, 2) + журнал решений
- `docs/superpowers/specs/` — спеки фаз
